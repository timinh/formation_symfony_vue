services:

###> containers ###
    ###> frankenphp ###
    app:
        extends:
            file: ../services/frankenphp.yaml
            service: frankenphp-local
        volumes:
            - ./entrypoint.sh:/tmp/entrypoint.sh
    ### frankenphp ###
    ###> mariadb ###
    db:
        extends:
            file: ../services/mariadb.yaml
            service: mariadb-local
    ### mariadb ###

    ###> nodejs ###
    nodejs:
        extends:
            file: ../services/nodejs.yaml
            service: nodejs-local
    ### nodejs ###

    maildev:
        extends:
            file: ../services/maildev.yaml
            service: maildev-local
### containers ###