<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ vite_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ vite_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <div id="app">
        Chargement...
    </div>
    {% if user_token %}
        <script language="javascript">
            const user_token = "{{ user_token|e('js') }}"
        </script>
    {% endif %}
     </body>
</html>
