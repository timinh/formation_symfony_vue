{% extends 'base.html.twig' %}

{% block title %}{{ project.title }}{% endblock %}

{% block body %}
    <h1>{{ project.title }}</h1>
    <p>{{ project.description }}</p>
    <a class="btn btn-primary" href="{{ path('app_project') }}">Retour</a>
{% endblock %}
