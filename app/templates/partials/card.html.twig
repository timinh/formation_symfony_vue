<div class="col-md-6 mb-4">
    <div class="card shadow-sm h-100">
        <div class="card-body">
            <p class="card-text fs-5 fst-italic">"{{ testimonial.content }}"</p>
            <div class="d-flex align-items-center mt-3">
                <div class="flex-shrink-0">
                    <img src="https://placehold.co/80" class="rounded-circle" alt="{{ testimonial.name }}">
                </div>
                <div class="flex-grow-1 ms-3">
                    <h5 class="card-title mb-0">{{ testimonial.name }}</h5>
                    <p class="card-subtitle text-muted">{{ testimonial.position }}</p>
                </div>
            </div>
        </div>
    </div>
</div>